let names = ['Anna', 'Jane', 'Joe', 'Bob', 'Sarah'];

names.pop();

names[1] = 'Sam';

names.push('Joakim');

names.sort();

names.indexOf('Joakim');

names.splice(2,1);

names.forEach(name => console.log(name));
